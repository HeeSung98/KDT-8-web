<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>get</title>
  </head>
  <body>
    <form action="/postResult" , method="post">
      이름: <input type="text" name="name" /><br /><br />
      성별:
      <input type="radio" name="sex" value="남" />남
      <input type="radio" name="sex" value="여" />여<br /><br />
      생년월일:
      <select name="y">
        <% for(var i = 1900; i <= 2023; i++) { %>
        <option value="<%=i%>"><%= i %></option>
        <% } %></select
      >년
      <select name="m">
        <% for(var i = 1; i <= 12; i++) { %>
        <option value="<%=i%>"><%= i %></option>
        <% } %></select
      >월
      <select name="d">
        <% for(var i = 1; i <= 31; i++) { %>
        <option value="<%=i%>"><%= i %></option>
        <% } %></select
      >일<br /><br />
      관심사: <input type="checkbox" value="여행" name="f" />여행
      <input type="checkbox" value="패션" name="f" />패션
      <input type="checkbox" value="음악" name="f" />음악 <br /><br />
      좋아하는 비밀번호: <input type="password" name="pw" /><br /><br />
      좋아하는 색: <input type="color" name="color" id="" /><br /><br />
      <button>회원가입</button>
    </form>
  </body>
</html>
